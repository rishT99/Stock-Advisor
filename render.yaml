services:
  - type: web
    name: stock-advisor-india
    env: docker
    plan: free
    # region: frankfurt         # ← remove; region is immutable once created
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    healthCheckPath: /healthz
